# GraphQL And File Upload

1. Use `POST` with `query`, `variables`, and `operationName` for GraphQL.
2. Assert GraphQL `errors` arrays even when HTTP status is `200`.
3. Use multipart helpers for file upload APIs.
4. Assert upload metadata, content type, and rejection of oversized files.
