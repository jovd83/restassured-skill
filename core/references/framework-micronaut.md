# Micronaut Recipe

1. Keep Micronaut environment activation in test resources or build settings.
2. Isolate HTTP client config from assertion logic.
3. Reuse Micronaut test bootstrap only when the repo already standardizes on it.
