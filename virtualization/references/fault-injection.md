# Fault Injection

1. Simulate `500`, `502`, `503`, and `504` responses.
2. Simulate connection resets and slow responses.
3. Simulate malformed JSON or XML when parser resilience matters.
4. Assert retries, fallback messages, and preserved idempotency.
